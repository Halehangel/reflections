You can make changes only to the files that need to be changed and add them with out the need to add the whole project plus it makes it easier to track changes between versions
I may include only the files affected by the logical change in each commit.
